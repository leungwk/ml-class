;; -*- mode: org -*-

Assignments for ml-class using python (numpy, scipy) rather than
octave. Intended to run in a python shell through python-mode.

Currently linear regression, logistic regression (but not multiclass
using OvA), and a neural net have been implemented.
